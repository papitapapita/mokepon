export default class Attack {
  constructor(id, name, type, icon, available) {
    this.id = id;
    this.name = name;
    this.type = type;
    this.icon = icon;
    this.available = available;
  }
}
